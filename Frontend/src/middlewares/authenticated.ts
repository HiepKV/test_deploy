// import { ROUTE_PAGE } from "../constants/routes";

const authenticated = () => {
  // if (loggedIn) {
  //   return next();
  // }

  // next(ROUTE_PAGE.AUTH.LOGIN)
};

export default authenticated;
